const colors = require('tailwindcss/colors');

const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {},
		fontFamily: {
			hand: 'Zeyada'
		}
	},
	plugins: []
};

module.exports = config;
